services:
  sitegen:
    build: .
    ports: !override []
    volumes: !override
      - ~/.claude/plans:/content:ro
      - site-output:/docs
      - ./cert.pem:/cert.pem
    command: !override [ "serve", "-src", "/content", "-out", "/docs", "-no-addr", "-verbose" ]
